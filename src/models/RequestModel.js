export class RequestModel {
    constructor(avatar, firstName, lastName, userID, userName, id) {
        this.avatar = avatar
        this.firstName = firstName
        this.lastName = lastName
        this.userID = userID
        this.userName = userName
        this.id = id
    }
}
